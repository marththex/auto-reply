# Credentials — never commit
credentials/*
!credentials/.gitkeep
token.json
client_secret*.json
.env

# Personal facts used for prompt grounding — never commit
facts.yaml
# Sender allowlist reveals personal contacts — never commit
allowlist.yaml
# Personal ops runbook — never commit
NOTES.md

# Unsloth build artifacts
unsloth_compiled_cache/

# Personal email data — never commit
data/*
!data/.gitkeep
*.mbox
*.jsonl

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.pytest_cache/

# Model artifacts (training comes later)
models/
checkpoints/
*.safetensors
*.gguf
